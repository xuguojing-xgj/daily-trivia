# js 中如何优化多个 if

- 示例

```js

if() {

} else if () {

} else if () {

} else {

}

```

## 对象字面量方式

```js
const statusMap = {
  key: "value",
};

const checkStatus = (status) => {
  return statusMap[status] || "Unknown";
};
```
